// Dato del tipo number

let edad = 32;
console.log(edad);

// Dato del tipo number con decimales

let precio = 32.20;
console.log(precio);

// Dato del tipo string

let nombre = "Pablo";
console.log(nombre);

// Dato del tipo string con comilla simple

let apellido = 'Camarotta';
console.log(apellido);

// Dato booleano con valor true

let verdadero = true;
console.log(verdadero);

// Dato booleano con valor false

let falso = false;
console.log(falso);

// Dato del tipo Nan

let notANumber = NaN;
console.log(notANumber);

// Dato del tipo Null

let nulo = null;
console.log(nulo);

// Dato Undefined

let sinValor = "Sin Valor";
console.log(sinValor);

// Dato del tipo objeto literal

let persona = {
    nombre: 'Pablo',
    edad: 32
}
console.log(persona);

// Dato del tipo Array

let paises = ['Argentina', 'España','Italia'];
console.log(paises);